// Production-optimized font loading with error handling and performance monitoring
export const loadFonts = () => {
  // Fonts are now loaded dynamically from the API
  // This function is kept for compatibility but doesn't load specific fonts
  return Promise.resolve([]);
};

export const preloadFonts = () => {
  // Fonts are now loaded dynamically from the API
  // This function is kept for compatibility
};

export const lazyLoadFonts = async () => {
  // Fonts are now loaded dynamically from the API
  // This function is kept for compatibility
  return Promise.resolve();
};